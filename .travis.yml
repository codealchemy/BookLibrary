language: ruby
rvm:
  - '2.5.3'
env:
  - DB=postgresql
before_script:
  - cd ruby
  - cp config/database.yml.travis config/database.yml
  - psql -c 'create database travis_ci_test;' -U postgres
before_install:
  - cd ruby && gem install bundler
